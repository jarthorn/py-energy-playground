# Objective

This is a simple set of command line tools for analyzing electricity generation data. Data is scraped from external sources into local files in the data directory. Other commands create various summaries and reports based on this data.

# Tech stack

- Python 3.15
- Ruff for python formatting. Please run ruff check and ruff format before committing.